//componente para poner en valor referentes femeninos, este será el home, con acceso a
//componente: deportes, política, arte, negocios, ciencias y activismo. Cada componente
//tendrá una foto (opcional), nombre, ocupación, breve biografía, y enlace a su página
//wikipedia, o sitio del que obtener más información.
import React from "react";
import { View, Text } from "react-native";

export default function Landmarks() {
  return (
    <View>
      <Text>Referentes</Text>
    </View>
  );
}
