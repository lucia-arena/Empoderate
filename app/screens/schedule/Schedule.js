//Componente de eventos. Tendrá un calendario en el que los usuarios
//logueados puedan añadir eventos relevantes para que todos los usuarios
//lo vean, y un botón para reportar algo inapropiado. Al añadir evento
// se añade el título, la localización, fecha, hora, y descripción.
import React from "react";
import { View, Text } from "react-native";

export default function Schedule() {
  return (
    <View>
      <Text>Eventos</Text>
    </View>
  );
}
